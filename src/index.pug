html
  head
    title ใครสปอนเซอร์ช่องฟ้า
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(name="theme-color", content="#27589d")
    meta(name="og:image", property="./assets/sharer.png")
    link(rel="shortcut icon", href="./assets/favicon.png", type="image/x-icon")
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Chonburi&display=swap")
    link(rel="stylesheet", href="./styles/main.sass")
  body
    header
      h1 ใครลงโฆษณา<span>ช่องฟ้า</span>
      a.button(href="") รายงานสปอนเซอร์
      a.button(href="") GitHub
    main
      .container
        .cards
          each p in sponsors
            .card
              .product= p.productName
              .by= p.companyName ? 'ของ ' + p.companyName : ''
    footer
      p ข้อมูล ณ วันที่ #{dataDate}

    script(async, src="https://www.googletagmanager.com/gtag/js?id=UA-49199645-8")
    script
      |window.dataLayer = window.dataLayer || [];
      |function gtag(){dataLayer.push(arguments);}
      |gtag('js', new Date());
      |gtag('config', 'UA-49199645-8');
